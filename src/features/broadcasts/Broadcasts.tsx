import React from 'react';
import {Text, View} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

const broadcasts = () => {
  return (
    <SafeAreaView>
      <Text>Broadcasts Page!</Text>
    </SafeAreaView>
  );
};

export default broadcasts;
